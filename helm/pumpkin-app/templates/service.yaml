apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-service
  labels:
    app: {{ .Release.Name }}
spec:
  type: ClusterIP # Security: Keep service internal to the cluster
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
  selector:
    app: {{ .Release.Name }}